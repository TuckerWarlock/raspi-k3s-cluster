name: Cluster Validation

on:
  pull_request:
    branches: [main]
    paths:
      - "cluster/**"
      - "bootstrap/scripts/install-argocd.sh"
      - ".github/workflows/helm-validate.yml"
      - ".github/workflows/reusable-manifest-validation.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  helm_validate:
    name: Helm and Schema Validation
    uses: TuckerWarlock/raspi-k3s-cluster/.github/workflows/reusable-manifest-validation.yml@main
    with:
      validator: kubeconform

  pluto_validate:
    name: API Deprecation Validation
    uses: TuckerWarlock/raspi-k3s-cluster/.github/workflows/reusable-manifest-validation.yml@main
    with:
      validator: pluto
      pluto_version: "5.23.5"
      k8s_target_version: "v1.31.0"
